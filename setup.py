from setuptools import setup, find_packages

setup(
    name='omega',
    version='0.1.0'
    packages=find_packages(include=['test_omega', 'teste', 'gerabase', 'dectobin', 'geramontador', 'mainu2', 'geradecimal', 'Omega', 'permuta'])
)
